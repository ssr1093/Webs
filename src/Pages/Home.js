import React from 'react';
import GifSlider from '../Components/GifSlider';

const Home = () => {
  return (
    <section className="home">
        <GifSlider />
    </section>
  );
};

export default Home;
